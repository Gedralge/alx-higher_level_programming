#!/usr/bin/python3

def print_reversed_list_integer(my_list =[]):
    """print all integers of a list in reverse order."""
    if my_list == None:
        return None
    if len(my_list)==0:
        print("{:d}".format(i))
